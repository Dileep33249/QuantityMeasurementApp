spring:
  application:
    name: user-service

server:
  port: ${PORT:8083}
  address: 0.0.0.0

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_URL:http://localhost:8761/eureka}
